# Only deployed with: make deploy-arcanna-rag ENV=baremetal-e2e
storagePvc:
  storageClassName: sc-e2e

logPvc:
  storageClassName: sc-e2e

# Model cache PVC (BGE-M3 + reranker + docling artifacts, ~5GB filled once).
models:
  pvc:
    storageClassName: sc-e2e
