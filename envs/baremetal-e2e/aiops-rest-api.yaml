image:
  repository: gcr.io/siscale-aiops/aiops-rest-api-release

# service.type / nodePort are flipped to NodePort at deploy time by the
# Makefile when BACKEND_URL is empty in .env.

samples:
  enabled: true
  profile: "dev"
