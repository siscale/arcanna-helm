# Only deployed with: make deploy-arcanna-rag ENV=@ENV_NAME@
storagePvc:
  storageClassName: @STORAGE_CLASS@

logPvc:
  storageClassName: @STORAGE_CLASS@

# Model cache PVC (BGE-M3 + reranker + docling artifacts, ~5GB filled once).
models:
  pvc:
    storageClassName: @STORAGE_CLASS@
