name: @ES_CLUSTER_NAME@
elasticsearchRef: @ES_CLUSTER_NAME@
# publicBaseUrl omitted — NodePort default. Set if you front Kibana with ingress.

resources:
  requests:
    memory: "512Mi"
    cpu: "250m"
  limits:
    memory: "1Gi"
    cpu: "1"
