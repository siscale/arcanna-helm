# backendUrl / monitoringUrl are resolved by the Makefile at deploy time,
# either from .env or (if empty) auto-built from NodePort + first node IP.
# Do not hardcode them here — .env is the one place to set them.

service:
  nodePort:
    enabled: true
    port: 31300
