serviceName: cacher
componentType: cacher
port: 9777

image:
  repository: gcr.io/siscale-aiops/component-webservice
  # Air-gap: never re-pull once internet is cut — use the node's cached image.
  pullPolicy: IfNotPresent

extraConfig:
  enabled: true
  name: cacher-config
  mountPath: /opt/arcanna/config/cacher/cacher.config
  subPath: cacher.config
