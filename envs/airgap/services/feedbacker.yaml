serviceName: feedbacker
componentType: feedbacker
port: 8888

image:
  repository: gcr.io/siscale-aiops/component-webservice
  # Air-gap: never re-pull once internet is cut — use the node's cached image.
  pullPolicy: IfNotPresent

extraConfig:
  enabled: true
  name: feedbacker-config
  mountPath: /opt/arcanna/config/feedbacker/feedbacker.config
  subPath: feedbacker.config
