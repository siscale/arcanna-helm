apiVersion: v2
name: admin-user
description: |
  One-shot Job that creates the Arcanna admin user in Elasticsearch security.
  Reuses the aiops-rest-api image. Hits ES via DATA_WAREHOUSE_* env vars
  derived from esSecretName (same key the rest-api chart takes).
type: application
version: 0.5.0
appVersion: "1.0.0"