apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: arcanna-admin-password
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: {{ .Values.secretStoreName }}
  target:
    name: arcanna-admin-password
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: {{ .Values.adminPasswordSecretName }}
        property: password
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None