apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "core-framework.fullname" . }}-log-pvc
  labels:
    {{- include "core-framework.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.logVolume.size }}
  {{- if .Values.logVolume.storageClassName }}
  storageClassName: {{ .Values.logVolume.storageClassName }}
  {{- end }}
